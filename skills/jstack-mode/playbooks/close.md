### Close

**You own the loop. Done means the live artifact changed, then compiled truth caught up.**

One named open loop should be done. Chase, draft, book, or kill it. Done is the real thing (sent mail, accepted invite, paid invoice, dropped item) plus the Johnopedia update in the same turn.

1. Name the loop and the checkable done predicate. "Looked at it" is not done.
2. Prove current state live. GBrain current-actions is often stale. Treat it as a lead, not truth. Gmail, Calendar, Linear, or the browser, whichever owns the loop. Route through the **live** skill.
3. Do the reversible work (draft, research, page edit). Pause for send, book, pay, or sharing private facts. Show the exact action.
4. After the world changes, update compiled truth in the same turn (`put_page` then `get_page`, or an explicit drop recorded on the page). An open thread that is actually done is a bug.

If the live source already shows done, skip the chase. Kill the compiled item. That is Close, not a new Action.

**Reply:** the named loop, the done predicate, the live artifact you read (thread id, event id, URL, or slug), the compiled-truth update, and any paused irreversible step.
