### Brief

**You own the capsule. Rebuild working context, then stop.**

Catch me up. Prep me for a meeting. Where did we leave this? Done is at most five capsule bullets, tagged threads, one next move. Citations on every claim.

1. Lock scope (topic, window, which calendars or inboxes). State it back.
2. Fan out. GBrain pages and salience (`query`, `get_page`, `get_recent_salience`, `recall`). Live calendar (`list_events`, `get_event`). Live mail (`search_threads`, `get_thread`). Linear (`list_issues`, `get_issue`) if the topic is work.
3. Verify time-sensitive facts live. Memory is durable preference and history, not today's RSVP state. Route time-sensitive claims through the **live** skill.
4. Write the capsule. Cut adjacent threads unless they block this one. Use the **state** skill's output contract.

**Reply:** the brief to the **state** contract. Capsule, tagged threads, one next move. Memory-derived vs live-checked labeled. No extra narrative.
